# NetGuard configuration

# AI provider configuration (set these in your environment or via Streamlit secrets).
# Never put a real API key in this file or commit a .env with secrets.

AI_API_URL=
AI_API_KEY=
